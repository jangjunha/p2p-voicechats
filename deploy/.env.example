# Your domain (Caddy fetches a TLS certificate for it automatically).
DOMAIN=chat.example.com

# Shared secret between vc-server and coturn for ephemeral TURN credentials.
# Generate with: openssl rand -hex 32
TURN_SECRET=change-me
